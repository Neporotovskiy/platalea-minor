import React from "react";
import "./skeleton.css";
type Props = {
    width: string;
    height: string;
    className?: string;
};
export declare const Skeleton: ({ width, height, className, ...other }: Props) => React.JSX.Element;
export {};
